#include <stdio.h>
int main()
{
   int i,j,flag=0,n;
    printf("ejldf");
    scanf("%d",&n);
    int ar[n];
    for (i=0;i<n;i++)
    {
        scanf("%d",&ar[i]);
    }
    for (i=(n-1);i>=0;i--)
    {
        flag=0;
        for (j=2;j<ar[i];j++)
        {
            if (ar[i]%j==0)
            {
                flag=1;
            }
        }
        if (flag==0)
        {
          printf("%d\n",ar[i]);
        }
    }
    return 0;
}
