#include <stdio.h>

int main() {
    int x=0,i;
    int ar[4];
    for (i=0;i<4;i++)
    {
        scanf("%d",&ar[i]);
    }
    for (i=0;i<4;i++)
    {
       if (ar[i]>=10)
        {
            x++;
        }
    }
    printf("%d",x);
    return 0;
}
